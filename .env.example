# ==================================================
# 城市传感器数据分析系统 - 环境变量配置
# ==================================================
# 复制此文件为 .env 并填入实际配置信息
# 注意：.env 文件包含敏感信息，不应提交到版本控制系统

# ==================================================
# 数据库配置
# ==================================================

# 数据库连接信息
DB_HOST=localhost
DB_PORT=3306
DB_USER=your_username
DB_PASSWORD=your_password
DB_NAME=sensor_database
DB_CHARSET=utf8mb4

# 传感器数据表名称
# 这是存储传感器JSON数据的主表
SENSOR_TABLE_NAME=sensor_data

# ==================================================
# LLM API 配置
# ==================================================

# Insight Agent (传感器数据分析)
INSIGHT_ENGINE_API_KEY=your_api_key_here
INSIGHT_ENGINE_BASE_URL=https://api.openai.com/v1
INSIGHT_ENGINE_MODEL_NAME=gpt-4

# Report Agent (报告生成)
REPORT_ENGINE_API_KEY=your_api_key_here
REPORT_ENGINE_BASE_URL=https://api.openai.com/v1
REPORT_ENGINE_MODEL_NAME=gpt-4

# Forum Engine (协作分析)
FORUM_ENGINE_API_KEY=your_api_key_here
FORUM_ENGINE_BASE_URL=https://api.openai.com/v1
FORUM_ENGINE_MODEL_NAME=gpt-4

# ==================================================
# 传感器配置
# ==================================================

# 默认查询限制
DEFAULT_QUERY_LIMIT=1000

# 统计分析配置
DEFAULT_STATISTICAL_HOURS=24
ANOMALY_THRESHOLD_STD_DEV=2.0

# ==================================================
# 系统配置
# ==================================================

# 输出目录
OUTPUT_DIR=./reports
LOG_DIR=./logs

# Flask服务器配置
FLASK_HOST=0.0.0.0
FLASK_PORT=5000
FLASK_DEBUG=False

# ==================================================
# 可选配置
# ==================================================

# 是否启用ForumEngine
ENABLE_FORUM_ENGINE=true

# 报告语言
REPORT_LANGUAGE=zh-CN

# 图表主题
CHART_THEME=default

# ==================================================
# 数据库表结构说明
# ==================================================
# 传感器数据表 (SENSOR_TABLE_NAME) 应该包含以下字段：
# - id: INT PRIMARY KEY AUTO_INCREMENT
# - sensor_data: JSON (包含多个传感器的数据)
# - timestamp: DATETIME (数据时间戳)
#
# sensor_data JSON 示例：
# {
#     "temperature": 25.5,
#     "humidity": 60.2,
#     "pressure": 1013.25,
#     "pm25": 35,
#     "pm10": 50,
#     "co2": 400,
#     "noise": 45,
#     "light": 500
# }
# ==================================================
